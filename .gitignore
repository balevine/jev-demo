.plans/
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
.env

# The built CLI.
cli/jev

# Local Claude Code settings. A shared .claude/settings.json still commits.
.claude/settings.local.json
.claude/.cc-writes/

# The Google OAuth client. Every user supplies their own, and it never ships.
credentials.json
